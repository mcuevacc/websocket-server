const authorizedDomains = ['vitalsign.com', 'localhost:4200', 'localhost:3000'];
const defaultSubdomain = '';
const defaultType = 'user';
const visitantType = 'guest';

module.exports = {
    authorizedDomains,
    defaultSubdomain,
    defaultType,
    visitantType
}