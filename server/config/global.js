const authorizedDomains = ['mcuevapps.com', 'vitalsign.com', 'localhost:4200'];
const defaultSubdomain = '';
const defaultType = 'user';
const visitantType = 'guest';

module.exports = {
    authorizedDomains,
    defaultSubdomain,
    defaultType,
    visitantType
}